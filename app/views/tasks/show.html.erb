<h1><%= @task.title %></h1>

<p><%= simple_format(@task.details) %></p>

<%= check_box_tag 'completed', 'completed', @task.completed, disabled: true %>
<label for="completed">This task is <%= @task.completed ? 'completed' : 'not completed yet' %></label>
<hr>
<%= link_to 'Back to list', tasks_path %>
